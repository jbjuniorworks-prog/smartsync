# URL base da SmartSync API — deve terminar em /api (o backend prefixa as rotas com /api)
# Dev local: o backend sobe na porta 5000 (veja backend/.env.example)
REACT_APP_API_URL=http://localhost:5000/api

# Nome da loja impresso no recibo (opcional; padrão: CenterCel)
REACT_APP_LOJA_NOME=CenterCel
